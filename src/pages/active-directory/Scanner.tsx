import React from 'react';
import ADScanner from '../../components/active-directory/ADScanner';

const ActiveDirectoryScanner: React.FC = () => {
  return <ADScanner />;
};

export default ActiveDirectoryScanner; 